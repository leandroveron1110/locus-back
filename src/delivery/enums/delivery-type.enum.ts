export enum DeliveryType {
  PICKUP = 'PICKUP', // El cliente retira en el local
  IN_HOUSE_DELIVERY = 'IN_HOUSE_DELIVERY', // Entrega propia del negocio
  EXTERNAL_DELIVERY = 'EXTERNAL_DELIVERY', // Cadetería externa
}
